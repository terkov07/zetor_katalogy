from django.http import HttpResponse


def library(request):
    # Placeholder for now — replaced with the real templated view in the next section.
    return HttpResponse("Zetor Katalógy — project skeleton is running.")